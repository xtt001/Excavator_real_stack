# Scripts

This directory is reserved for full-stack bring-up and deployment helpers.

Examples to add later:

- Dependency checks for Python, CMake, CAN tools, and ROS.
- Mock bridge smoke test.
- CAN-disabled bridge smoke test.
- One-axis low-speed hardware check.
- Recorder launch wrapper for real-machine data collection.

Avoid putting hardware-facing scripts here until their safety assumptions and
operator checklist are documented.

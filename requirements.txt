# Python package dependencies are owned by testbed/pyproject.toml.
# Install from the repository root:
#   python -m pip install -r requirements.txt
#
# System ROS2 dependencies are not pip packages:
# - slave / Ubuntu 22.04: ROS2 Humble is the hardware-side baseline.
# - host / Ubuntu 24.04: ROS2 Jazzy is optional, only for local rqt viewing:
#   sudo apt install -y ros-jazzy-ros-base ros-jazzy-rqt-image-view \
#     ros-jazzy-image-transport-plugins ros-jazzy-compressed-image-transport \
#     ros-jazzy-cv-bridge ros-jazzy-rmw-cyclonedds-cpp
-e ./testbed
pytest>=8.0
